<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <EditText
        android:id="@+id/et_target_url"
        android:layout_width="393dp"
        android:layout_height="71dp"
        android:hint="Ingrese la URL objetivo"
        android:inputType="textUri" />

    <EditText
        android:id="@+id/et_iterations"
        android:layout_width="387dp"
        android:layout_height="70dp"
        android:layout_below="@id/et_target_url"
        android:layout_marginTop="16dp"
        android:hint="Ingrese el número de paquetes"
        android:inputType="number" />

    <Button
        android:id="@+id/btn_realizar_dos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Realizar Ataque DoS"
        android:layout_below="@id/et_iterations"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp" />

    <Button
        android:id="@+id/btn_back_to_main"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Regresar al Menú Principal"
        android:layout_below="@id/btn_realizar_dos"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp" />

</RelativeLayout>
